# 一、为什么新增节点没纳入图中？
你说得对！原代码的`_build_graph`方法只添加了原有5个节点，**新增的5个节点（环境感知、代码生成、代码执行等）确实没有被纳入工作流图中**。原因是：
1. **原代码是基础版本**：只实现了“固定工具扫描”的核心流程，新增节点是后续扩展的功能，需要手动将其添加到图中；
2. **缺少分支逻辑设计**：新增节点需要“条件触发”（比如“需要自定义扫描时才执行代码生成”），原代码只有简单的线性+循环逻辑，没有处理这种复杂分支；
3. **入口点未调整**：环境感知节点应该作为工作流的“第一个执行节点”（先感知环境，再做规划），原代码入口是任务规划节点。

---

# 二、完整修改方案：把新增节点纳入图中（实现Trae AIIDE式自主扫描）
我会提供**修改后的完整代码**，核心目标是：
1. **环境感知前置**：工作流从“环境感知”开始，先获取系统信息；
2. **智能决策嵌入**：在任务规划后添加“智能决策”，决定是否需要自定义扫描/功能增强；
3. **条件分支实现**：通过LangGraph的`add_conditional_edges`实现“固定工具扫描”和“代码生成扫描”的分支；
4. **代码生成→执行→补充闭环**：生成代码后执行，执行失败时触发功能补充（比如自动安装依赖）。

## 2.1 修改后的完整代码（LangGraph图构建+工具初始化）
```python
"""
LangGraph 图构建（修改版）

构建支持自主规划、代码生成、环境感知的完整Agent工作流。
"""
import logging
from typing import Dict, Any, Literal
from langgraph.graph import StateGraph, END

from .state import AgentState
from .nodes import (
    TaskPlanningNode,
    ToolExecutionNode,
    ResultVerificationNode,
    VulnerabilityAnalysisNode,
    ReportGenerationNode
)
from .new_nodes import (
    EnvironmentAwarenessNode,
    CodeGenerationNode,
    CapabilityEnhancementNode,
    CodeExecutionNode,
    IntelligentDecisionNode
)
from ..agent_config import agent_config

logger = logging.getLogger(__name__)


class ScanAgentGraph:
    """
    扫描Agent图类（支持自主代码生成的增强版）
    """
    
    def __init__(self):
        """
        初始化扫描Agent图（包含所有新增节点）
        """
        logger.info("🔧 初始化增强版扫描Agent图")
        
        # 1. 创建所有节点实例（原有+新增）
        self.env_awareness_node = EnvironmentAwarenessNode()  # 环境感知
        self.planning_node = TaskPlanningNode()  # 任务规划
        self.intelligent_decision_node = IntelligentDecisionNode()  # 智能决策
        self.execution_node = ToolExecutionNode()  # 固定工具执行
        self.code_generation_node = CodeGenerationNode()  # 代码生成
        self.code_execution_node = CodeExecutionNode()  # 代码执行
        self.capability_enhancement_node = CapabilityEnhancementNode()  # 功能补充
        self.verification_node = ResultVerificationNode()  # 结果验证
        self.analysis_node = VulnerabilityAnalysisNode()  # 漏洞分析
        self.report_node = ReportGenerationNode()  # 报告生成
        
        # 2. 构建增强版工作流图
        self.graph = self._build_enhanced_graph()
        
        logger.info("✅ 增强版扫描Agent图构建完成")
    
    def _build_enhanced_graph(self) -> StateGraph:
        """
        构建支持自主代码生成的增强版LangGraph图
        Returns:
            StateGraph: 编译后的图
        """
        # 1. 创建状态工作流图（数据载体是AgentState）
        workflow = StateGraph(AgentState)
        
        # 2. 添加所有节点（原有+新增）
        workflow.add_node("environment_awareness", self.env_awareness_node)  # 新增
        workflow.add_node("task_planning", self.planning_node)
        workflow.add_node("intelligent_decision", self.intelligent_decision_node)  # 新增
        workflow.add_node("tool_execution", self.execution_node)
        workflow.add_node("code_generation", self.code_generation_node)  # 新增
        workflow.add_node("code_execution", self.code_execution_node)  # 新增
        workflow.add_node("capability_enhancement", self.capability_enhancement_node)  # 新增
        workflow.add_node("result_verification", self.verification_node)
        workflow.add_node("vulnerability_analysis", self.analysis_node)
        workflow.add_node("report_generation", self.report_node)
        
        # 3. 设置入口点：从环境感知开始（先看环境，再做规划）
        workflow.set_entry_point("environment_awareness")
        
        # 4. 基础流程：环境感知→任务规划→智能决策
        workflow.add_edge("environment_awareness", "task_planning")
        workflow.add_edge("task_planning", "intelligent_decision")
        
        # 5. 核心条件分支1：智能决策后选择“固定工具”或“代码生成”
        workflow.add_conditional_edges(
            "intelligent_decision",  # 起始节点：智能决策
            self._decide_scan_type,  # 分支判断函数
            {
                "fixed_tool": "tool_execution",  # 用现有工具扫描
                "custom_code": "code_generation",  # 生成代码扫描
                "enhance_first": "capability_enhancement"  # 先增强功能再扫描
            }
        )
        
        # 6. 代码生成→执行→补充闭环
        workflow.add_edge("code_generation", "code_execution")
        # 代码执行失败时，触发功能补充（比如自动安装依赖）
        workflow.add_conditional_edges(
            "code_execution",
            self._code_execution_result,
            {
                "success": "result_verification",  # 执行成功→验证结果
                "need_enhance": "capability_enhancement"  # 执行失败→功能补充
            }
        )
        # 功能补充后回到代码执行（重试）
        workflow.add_edge("capability_enhancement", "code_execution")
        
        # 7. 固定工具流程：执行→验证→循环/分析
        workflow.add_edge("tool_execution", "result_verification")
        # 结果验证后的分支：有任务继续执行，无任务进入分析
        workflow.add_conditional_edges(
            "result_verification",
            self._should_continue,
            {
                "continue": "tool_execution",  # 继续执行工具
                "analyze": "vulnerability_analysis"  # 进入漏洞分析
            }
        )
        
        # 8. 后续流程：分析→报告→结束
        workflow.add_edge("vulnerability_analysis", "report_generation")
        workflow.add_edge("report_generation", END)
        
        logger.info("📊 增强版LangGraph图边定义完成")
        return workflow
    
    def _decide_scan_type(self, state: AgentState) -> Literal["fixed_tool", "custom_code", "enhance_first"]:
        """
        智能决策：选择扫描类型（核心分支逻辑）
        Returns:
            扫描类型：固定工具/自定义代码/先增强功能
        """
        target_context = state.target_context
        # 1. 需要功能增强（比如依赖缺失）→先增强
        if target_context.get("need_capability_enhancement"):
            logger.info(f"[{state.task_id}] 🚀 需要功能增强，优先执行增强节点")
            return "enhance_first"
        # 2. 需要自定义扫描→生成代码
        elif target_context.get("need_custom_scan"):
            logger.info(f"[{state.task_id}] 🔧 需要自定义扫描，执行代码生成")
            return "custom_code"
        # 3. 其他情况→使用现有工具
        else:
            logger.info(f"[{state.task_id}] 🛠️ 使用现有工具执行扫描")
            return "fixed_tool"
    
    def _code_execution_result(self, state: AgentState) -> Literal["success", "need_enhance"]:
        """
        判断代码执行结果：成功→继续流程，失败→触发功能补充
        """
        execution_result = state.tool_results.get("code_execution", {})
        if execution_result.get("status") == "success":
            return "success"
        else:
            # 执行失败时，标记需要功能增强
            state.target_context["need_capability_enhancement"] = True
            state.target_context["capability_requirement"] = "自动安装代码执行所需依赖"
            return "need_enhance"
    
    def _should_continue(self, state: AgentState) -> Literal["continue", "analyze"]:
        """
        判断是否继续执行工具（原有逻辑，保持不变）
        """
        if state.is_complete or not state.planned_tasks:
            logger.info(f"[{state.task_id}] 📋 所有任务已完成，进入分析阶段")
            return "analyze"
        else:
            logger.info(f"[{state.task_id}] 🔄 继续执行工具: {state.current_task}")
            return "continue"
    
    def compile(self):
        """编译图（保持不变）"""
        return self.graph.compile()
    
    async def invoke(self, initial_state: AgentState) -> AgentState:
        """执行工作流（保持不变）"""
        logger.info(f"🚀 开始执行增强版Agent工作流: {initial_state.task_id}")
        compiled_graph = self.compile()
        final_state = await compiled_graph.ainvoke(initial_state)
        logger.info(f"✅ 增强版Agent工作流执行完成: {final_state.task_id}")
        return final_state
    
    def get_graph_info(self) -> Dict[str, Any]:
        """获取图信息（新增节点已添加）"""
        return {
            "nodes": [
                "environment_awareness", "task_planning", "intelligent_decision",
                "tool_execution", "code_generation", "code_execution",
                "capability_enhancement", "result_verification",
                "vulnerability_analysis", "report_generation"
            ],
            "edges": [
                ("environment_awareness", "task_planning"),
                ("task_planning", "intelligent_decision"),
                ("intelligent_decision", "tool_execution"),
                ("intelligent_decision", "code_generation"),
                ("intelligent_decision", "capability_enhancement"),
                ("code_generation", "code_execution"),
                ("code_execution", "result_verification"),
                ("code_execution", "capability_enhancement"),
                ("capability_enhancement", "code_execution"),
                ("tool_execution", "result_verification"),
                ("result_verification", "tool_execution"),
                ("result_verification", "vulnerability_analysis"),
                ("vulnerability_analysis", "report_generation"),
                ("report_generation", "END")
            ],
            "entry_point": "environment_awareness",
            "exit_points": ["report_generation"]
        }
    
    def visualize(self) -> str:
        """生成Mermaid可视化图（直观看到完整流程）"""
        return """
graph TD
    A[环境感知] --> B[任务规划]
    B --> C[智能决策]
    C -->|固定工具| D[工具执行]
    C -->|自定义代码| E[代码生成]
    C -->|需要增强| F[功能补充]
    E --> G[代码执行]
    G -->|成功| H[结果验证]
    G -->|失败| F
    F --> G
    D --> H
    H -->|有任务| D
    H -->|无任务| I[漏洞分析]
    I --> J[报告生成]
    J --> K[结束]
    
    style A fill:#e1f5ff
    style B fill:#fff3cd
    style C fill:#e8f5e9
    style D fill:#fff3cd
    style E fill:#f8d7da
    style F fill:#fce4ec
    style G fill:#f8d7da
    style H fill:#d4edda
    style I fill:#f8d7da
    style J fill:#fce4ec
    style K fill:#c3e6cb
        """


def create_agent_graph() -> ScanAgentGraph:
    """创建增强版Agent图实例（保持不变）"""
    return ScanAgentGraph()


def initialize_tools():
    """初始化所有工具（保持不变，确保工具都能被调用）"""
    from ..tools.registry import registry
    from ..tools.adapters import PluginAdapter, POCAdapter
    
    logger.info("🔧 开始初始化工具...")
    
    # 注册插件类工具
    registry.register(
        name="baseinfo",
        func=PluginAdapter.adapt_baseinfo(),
        description="基础信息收集（域名、IP、服务器、OS等）",
        category="plugin",
        timeout=10,
        priority=3
    )
    registry.register(
        name="portscan",
        func=PluginAdapter.adapt_portscan(),
        description="TCP端口扫描，识别开放端口和服务",
        category="plugin",
        timeout=120,
        priority=5
    )
    registry.register(
        name="waf_detect",
        func=PluginAdapter.adapt_waf_detect(),
        description="WAF（Web应用防火墙）检测",
        category="plugin",
        timeout=10,
        priority=4
    )
    registry.register(
        name="cdn_detect",
        func=PluginAdapter.adapt_cdn_detect(),
        description="CDN（内容分发网络）检测",
        category="plugin",
        timeout=10,
        priority=4
    )
    registry.register(
        name="cms_identify",
        func=PluginAdapter.adapt_cms_identify(),
        description="CMS（内容管理系统）识别",
        category="plugin",
        timeout=15,
        priority=4
    )
    registry.register(
        name="infoleak_scan",
        func=PluginAdapter.adapt_infoleak_scan(),
        description="信息泄露扫描",
        category="plugin",
        timeout=30,
        priority=3
    )
    registry.register(
        name="subdomain_scan",
        func=PluginAdapter.adapt_subdomain_scan(),
        description="子域名枚举",
        category="plugin",
        timeout=60,
        priority=3
    )
    
    # 注册POC类工具
    pocs = POCAdapter.get_all_pocs()
    for poc_name, poc_module in pocs.items():
        poc_wrapper = POCAdapter.adapt_poc(poc_name, poc_module)
        registry.register(
            name=poc_name,
            func=poc_wrapper,
            description=f"POC验证: {poc_name}",
            category="poc",
            timeout=30,
            priority=8
        )
    
    logger.info(f"✅ 工具初始化完成，共注册 {len(registry.tools)} 个工具")
```

## 2.2 核心修改点详细说明（零基础懂）
### （1）新增3个分支判断函数（实现Trae AIIDE式自主决策）
| 函数名 | 作用 | 返回值 | 应用场景 |
|--------|------|--------|----------|
| `_decide_scan_type` | 智能选择扫描类型 | "fixed_tool"/"custom_code"/"enhance_first" | 环境信息+目标特征→决定用现有工具还是生成代码 |
| `_code_execution_result` | 判断代码执行结果 | "success"/"need_enhance" | 代码执行失败→触发功能补充（自动安装依赖） |
| `_should_continue` | 判断是否继续执行工具 | "continue"/"analyze" | 有任务→继续执行，无任务→进入漏洞分析 |

### （2）关键流程修改（实现“自主规划→决策→生成→执行→补充”闭环）
1. **入口调整**：工作流从`environment_awareness`开始，先获取环境信息（比如“是否有nmap”“Python版本”），避免后续调用不存在的工具；
2. **智能决策嵌入**：在任务规划后添加`intelligent_decision`节点，根据环境+目标特征决定扫描方式（比如“目标是WebLogic且无现成POC→生成自定义代码”）；
3. **代码生成分支**：需要自定义扫描时，执行`code_generation`→`code_execution`，执行失败时自动触发`capability_enhancement`（比如生成的代码需要`requests`库，自动安装）；
4. **功能补充闭环**：`capability_enhancement`执行后回到`code_execution`，重试执行代码，形成“失败→增强→重试”的闭环；
5. **结果验证统一**：固定工具执行和代码执行的结果都进入`result_verification`，统一判断是否继续执行任务。

### （3）Mermaid可视化图（直观理解完整流程）
复制`visualize`方法返回的Mermaid代码到https://mermaid.live/，可生成以下流程图：
```
graph TD
    A[环境感知] --> B[任务规划]
    B --> C[智能决策]
    C -->|固定工具| D[工具执行]
    C -->|自定义代码| E[代码生成]
    C -->|需要增强| F[功能补充]
    E --> G[代码执行]
    G -->|成功| H[结果验证]
    G -->|失败| F
    F --> G
    D --> H
    H -->|有任务| D
    H -->|无任务| I[漏洞分析]
    I --> J[报告生成]
    J --> K[结束]
```

---

# 三、如何使用增强版Agent（实现Trae AIIDE式扫描）
## 3.1 快速使用步骤
```python
# 1. 初始化工具（注册所有插件和POC）
initialize_tools()

# 2. 创建增强版Agent图实例
agent_graph = create_agent_graph()

# 3. 定义初始状态（指定扫描目标，设置上下文）
initial_state = AgentState(
    task_id="scan_001",
    target="http://example.com",
    target_context={
        "need_custom_scan": True,  # 标记需要自定义扫描
        "custom_scan_type": "vuln_scan",
        "custom_scan_requirements": "检测WebLogic CVE-2020-2551漏洞"
    }
)

# 4. 执行工作流（自动完成“环境感知→决策→生成→执行→补充→报告”）
final_state = await agent_graph.invoke(initial_state)

# 5. 获取结果（漏洞列表+扫描报告）
print("发现漏洞：", final_state.vulnerabilities)
print("扫描报告：", final_state.tool_results["final_report"])
```

## 3.2 核心能力实现（和Trae AIIDE一致）
| 能力 | 实现方式 | 示例场景 |
|------|----------|----------|
| **自主规划** | `TaskPlanningNode`+`IntelligentDecisionNode` | 目标是WebLogic→规划“先扫端口→再生成WebLogic POC” |
| **工具扩充** | `CodeGenerationNode` | 无现成POC→生成Python漏洞检测脚本 |
| **自主执行** | `CodeExecutionNode`（沙箱安全执行） | 生成的脚本→在隔离环境中执行，避免影响主系统 |
| **动态增强** | `CapabilityEnhancementNode` | 脚本需要`requests`库→自动安装依赖 |
| **结果闭环** | `ResultVerificationNode` | 执行结果→验证漏洞是否存在→补充新任务 |

---

# 四、总结：修改后Agent的核心价值
1. **完全自主化**：从“环境感知→任务规划→智能决策→执行→生成→补充→报告”全流程自动执行，无需人工干预；
2. **工具+代码双模式**：既可以使用现有工具（nmap、POC），又能通过大模型生成自定义代码，适配“无现成工具”的场景；
3. **安全执行**：代码执行节点开启沙箱，避免恶意代码/错误代码影响Agent主环境；
4. **动态增强**：自动检测并解决依赖缺失、工具不存在等问题，提升扫描成功率；
5. **可扩展性强**：新增节点通过“条件分支”接入，后续可添加更多功能（比如“人工审核节点”“报告优化节点”）。

现在你的Agent已经具备Trae AIIDE的核心能力：**自主规划、自主决策、自主生成代码、自主执行测试**，既能用现有工具高效扫描，又能应对复杂的“无现成工具”场景，真正实现Web安全漏洞的全自动扫描。

需要我把上述修改后的代码整理成可直接运行的最小示例（含 AgentState 基类、必要配置和测试入口），让你能一键运行验证吗？